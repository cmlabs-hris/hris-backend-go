# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=hris_user
DB_PASSWORD=your_password
DB_NAME=hris_db
DB_SSL_MODE=disable
TEST_DATABASE_URL=postgres://hris_user:your_password@localhost:5432/hris_db_test?sslmode=disable

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Application Configuration
APP_PORT=8080
APP_ENV=development
LOG_LEVEL=info

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_EXPIRATION_TIME=24h
JWT_ACCESS_EXPIRATION_TIME=1h

# Session Configuration
SESSION_SECRET=your-session-secret-key
SESSION_TIMEOUT=30m

# Google OAuth2
CLIENT_ID=your-google-client-id
CLIENT_SECRET=your-google-client-secret
REDIRECT_URL=http://localhost:8080/api/v1/auth/oauth/callback/google
SCOPES=email

# Storage Configuration
STORAGE_TYPE=local
BASE_PATH=./storage
BASE_URL=http://localhost:8080/uploads
# MinIO/S3 config (uncomment when ready to migrate)
# MINIO_ENDPOINT=localhost:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_BUCKET=hris-files
# MINIO_USE_SSL=false

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=your-email@gmail.com
SMTP_FROM_NAME=HRIS Application

# Xendit Configuration
XENDIT_API_KEY=your-xendit-api-key
XENDIT_WEBHOOK_TOKEN=your-xendit-webhook-token
XENDIT_BASE_URL=https://api.xendit.co
XENDIT_ENVIRONMENT=sandbox
XENDIT_INVOICE_EXPIRY_HOURS=24
XENDIT_SUCCESS_REDIRECT=http://localhost:3000/subscription/success
XENDIT_FAILURE_REDIRECT=http://localhost:3000/subscription/failed

# Invitation Configuration
INVITATION_BASE_URL=http://localhost:3000

# Frontend URL
FRONTEND_URL=http://localhost:3000